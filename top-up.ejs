<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Topup - Page</title>

  <link rel="stylesheet" href="/css/style.css" />
  <!-- SweetAlert2 -->

  <style>
    body {
      margin-top: 120px;
    }

    h2 {
      text-align: center;
      margin-top: 40px;
      color: #007bff;
      font-weight: 700;
    }

    .message {
      max-width: 360px;
      margin: 15px auto;
      padding: 12px 15px;
      border-radius: 6px;
      font-weight: 600;
      font-size: 1rem;
      text-align: center;
    }

    .message.error {
      color: #842029;
      border: 1px solid #f5c2c7;
    }

    .message.success {
      color: #0f5132;
      border: 1px solid #badbcc;
    }

    form {
      display: flex;
      flex-direction: column;
      align-items: center;
      margin-top: 30px;
      gap: 12px;
    }

    .instructions {
      max-width: 360px;
      margin: 24px auto 0 auto;
      background-color: #f8f9fa;
      border-left: 4px solid #007bff;
      padding: 12px 16px;
      font-size: 0.95rem;
      border-radius: 6px;
      color: #333;
      line-height: 1.6;
    }



    input[type="text"] {
      width: 320px;
      padding: 12px 15px;
      font-size: 1.1rem;
      border: 1.8px solid #ced4da;
      border-radius: 6px;
      transition: border-color 0.3s ease;
    }

    input[type="text"]:focus {
      outline: none;
      border-color: #007bff;
      box-shadow: 0 0 6px #007bffaa;
    }

    form button {
      width: 160px;
      padding: 12px 20px;
      background-color: #007bff;
      color: white;
      font-size: 1.1rem;
      font-weight: 700;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      transition: background-color 0.25s ease;
    }

    form button:hover {
      background-color: #0056b3;
    }


    footer {
      margin-top: auto;
    }
  </style>
</head>

<body>
  <%- include('./partials/navbar') %>

    <h2>Top Up Using Red Packet Link</h2>

    <form method="POST" action="/top-up/redeem">
      <input type="text" name="link" placeholder="Paste your Red Packet link here" required />
      <button type="submit">Top Up</button>
    </form>

    <div class="instructions">
      <p>
        📌 <strong>How to create a Red Packet link (Send to one person):</strong><br />
        1. Open the <strong>TrueMoney Wallet</strong> app.<br />
        2. Go to the <strong>"Red Packet"</strong> or <strong>"Ang Pao"</strong> menu.<br />
        3. Select <strong>"Send to one person"</strong>.<br />
        4. Enter the amount you want to send, e.g., 50, 100 Baht.<br />
        5. Confirm and copy the generated link.<br />
        6. Paste the link into the input box above and click <strong>"Top Up"</strong>.
      </p>
    </div>


    <%- include('./partials/footer') %>

      <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

      <script>
    <% if (success) { %>
          Swal.fire({
            icon: 'success',
            title: '🎉 Success',
            text: <% - JSON.stringify(success) %>,
            confirmButtonText: 'OK',
            timer: 4000,
            timerProgressBar: true
          });
    <% } else if (error) { %>
          Swal.fire({
            icon: 'error',
            title: 'Error Occurred',
            text: <% - JSON.stringify(error) %>,
            confirmButtonText: 'OK',
            timer: 5000,
            timerProgressBar: true
          });
    <% } %>
      </script>
</body>

</html>